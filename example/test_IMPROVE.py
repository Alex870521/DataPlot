from DataPlot import *

df = DataBase

# plot.fRH_plot()
# plot.ammonium_rich(df)
# plot.wind_rose(df['WS'], df['WD'])
# plot.MLR_IMPROVE()

from tqdm import tqdm, trange
from colorama import Fore
import time


for i in trange(1000, colour="GREEN"):
    time.sleep(0.1)

# for i in trange(int(), bar_format="{l_bar}%s{bar}%s{r_bar}" % (Fore.BLUE, Fore.RESET)):
    # pass
